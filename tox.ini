[coverage:run]
omit = .tox/*

[testenv]
basepython = python3.9
deps = -r{toxinidir}/manager-requirements.txt

[testenv:utests]
deps =
  pip==21.1.2
  pytest==6.2.5

commands = pytest {posargs} {toxinidir}/runners_manager/
